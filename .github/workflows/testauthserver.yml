name: Test Authserver

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Install dependencies
      run: |
        apt install httpie
    - name: Run tests
      run: |
        make testauthserver
